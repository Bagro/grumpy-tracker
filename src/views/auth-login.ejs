<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= t('login') %> - Grumpy Tracker</title>
    <link href="/public/tailwind.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 min-h-screen flex flex-col">
    <header class="bg-blue-900 text-white p-4">
      <h1 class="text-2xl font-bold">Grumpy Tracker</h1>
    </header>
    <main class="flex-1 p-4 max-w-md mx-auto">
      <form method="post" action="/login" class="bg-white shadow p-6 rounded space-y-4">
        <h2 class="text-xl font-semibold mb-2"><%= t('login') %></h2>
        <% if (error) { %>
          <div class="bg-red-100 text-red-700 p-2 rounded"><%= error %></div>
        <% } %>
        <div>
          <label for="email" class="block mb-1"><%= t('email') %></label>
          <input id="email" name="email" type="email" required class="w-full border rounded p-2" autocomplete="username" />
        </div>
        <div>
          <label for="password" class="block mb-1"><%= t('password') %></label>
          <input id="password" name="password" type="password" required class="w-full border rounded p-2" autocomplete="current-password" />
        </div>
        <button type="submit" class="w-full bg-blue-700 text-white py-2 rounded hover:bg-blue-800"><%= t('login') %></button>
        <p class="text-sm mt-2"><a href="/register" class="text-blue-700 underline"><%= t('register') %></a></p>
      </form>
    </main>
    <footer class="bg-gray-200 text-center p-2 text-xs">&copy; <%= new Date().getFullYear() %> Grumpy Tracker</footer>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= t('register') %> - Grumpy Tracker</title>
    <link href="/public/tailwind.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 min-h-screen flex flex-col">
    <header class="bg-blue-900 text-white p-4">
      <h1 class="text-2xl font-bold">Grumpy Tracker</h1>
    </header>
    <main class="flex-1 p-4 max-w-md mx-auto">
      <form method="post" action="/register" class="bg-white shadow p-6 rounded space-y-4">
        <h2 class="text-xl font-semibold mb-2"><%= t('register') %></h2>
        <% if (error) { %>
          <div class="bg-red-100 text-red-700 p-2 rounded"><%= error %></div>
        <% } %>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div>
          <label for="name" class="block mb-1"><%= t('name') %></label>
          <input id="name" name="name" type="text" required class="w-full border rounded p-2" />
        </div>
        <div>
          <label for="email" class="block mb-1"><%= t('email') %></label>
          <input id="email" name="email" type="email" required class="w-full border rounded p-2" autocomplete="username" />
        </div>
        <div>
          <label for="password" class="block mb-1"><%= t('password') %></label>
          <input id="password" name="password" type="password" required class="w-full border rounded p-2" autocomplete="new-password" />
        </div>
        <div>
          <label for="language" class="block mb-1"><%= t('language') %></label>
          <select id="language" name="language" class="w-full border rounded p-2">
            <option value="en">English</option>
            <option value="sv">Svenska</option>
            <option value="fi">Suomi</option>
            <option value="no">Norsk</option>
            <option value="lv">Latviešu</option>
            <option value="et">Eesti</option>
            <option value="lt">Lietuvių</option>
            <option value="da">Dansk</option>
          </select>
        </div>
        <button type="submit" class="w-full bg-blue-700 text-white py-2 rounded hover:bg-blue-800"><%= t('register') %></button>
        <p class="text-sm mt-2"><a href="/login" class="text-blue-700 underline"><%= t('login') %></a></p>
      </form>
    </main>
    <footer class="bg-gray-200 text-center p-2 text-xs">&copy; <%= new Date().getFullYear() %> Grumpy Tracker</footer>
  </body>
</html>

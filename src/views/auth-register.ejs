<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= t('register') %> - Grumpy Tracker</title>
    <link href="/public/tailwind.css" rel="stylesheet">
  </head>
  <body class="bg-grumpy-dark min-h-screen flex flex-col font-sans text-grumpy-offwhite">
    <header class="bg-grumpy-dark2 text-grumpy-offwhite p-4 shadow-lg flex items-center gap-4">
      <img src="/public/logo.png" alt="Grumpy Tracker Logo" class="h-10 w-10 rounded-full bg-grumpy-dark2 border-2 border-grumpy-orange shadow" />
      <h1 class="text-2xl font-bold tracking-tight">Grumpy Tracker</h1>
    </header>
    <main class="flex-1 p-4 max-w-md mx-auto w-full">
      <form method="post" action="/register" class="bg-grumpy-dark2 shadow-2xl p-6 rounded-2xl space-y-4 border border-grumpy-orange">
        <h2 class="text-xl font-semibold mb-2 text-grumpy-orange"><%= t('register') %></h2>
        <% if (error) { %>
          <div class="bg-red-100 text-red-700 p-2 rounded"><%= error %></div>
        <% } %>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div>
          <label for="name" class="block mb-1"><%= t('name') %></label>
          <input id="name" name="name" type="text" required class="w-full border rounded p-2 bg-grumpy-dark text-grumpy-offwhite" />
        </div>
        <div>
          <label for="email" class="block mb-1"><%= t('email') %></label>
          <input id="email" name="email" type="email" required class="w-full border rounded p-2 bg-grumpy-dark text-grumpy-offwhite" autocomplete="username" />
        </div>
        <div>
          <label for="password" class="block mb-1"><%= t('password') %></label>
          <input id="password" name="password" type="password" required class="w-full border rounded p-2 bg-grumpy-dark text-grumpy-offwhite" autocomplete="new-password" />
        </div>
        <div>
          <label for="language" class="block mb-1"><%= t('language') %></label>
          <select id="language" name="language" class="w-full border rounded p-2 bg-grumpy-dark text-grumpy-offwhite">
            <option value="en">English</option>
            <option value="sv">Svenska</option>
            <option value="fi">Suomi</option>
            <option value="no">Norsk</option>
            <option value="lv">Latviešu</option>
            <option value="et">Eesti</option>
            <option value="lt">Lietuvių</option>
            <option value="da">Dansk</option>
          </select>
        </div>
        <button type="submit" class="w-full bg-grumpy-orange text-grumpy-offwhite py-2 rounded hover:bg-grumpy-dark2 hover:text-grumpy-orange border border-grumpy-orange transition font-semibold"><%= t('register') %></button>
        <p class="text-sm mt-2"><a href="/login" class="text-grumpy-orange underline hover:text-grumpy-offwhite transition"><%= t('login') %></a></p>
      </form>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>

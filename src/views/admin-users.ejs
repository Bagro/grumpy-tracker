<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin: Users - Grumpy Tracker</title>
  <link href="/public/tailwind.css" rel="stylesheet">
</head>
<body class="bg-grumpy-dark min-h-screen flex flex-col font-sans text-grumpy-offwhite">
  <header class="bg-grumpy-dark2 text-grumpy-offwhite p-4 shadow-lg flex items-center gap-4">
    <img src="/public/logo.png" alt="Grumpy Tracker Logo" class="h-10 w-10 rounded-full bg-grumpy-dark2 border-2 border-grumpy-orange shadow" />
    <h1 class="text-2xl font-bold tracking-tight">Admin: User Management</h1>
  </header>
  <main class="flex-1 p-4 max-w-2xl mx-auto w-full">
    <table class="w-full table-auto border mb-4 bg-grumpy-dark2 text-grumpy-offwhite border-grumpy-orange rounded-2xl shadow">
      <thead>
        <tr class="bg-grumpy-dark">
          <th>Name</th>
          <th>Email</th>
          <th>Language</th>
          <th>Created</th>
          <th>Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(u => { %>
          <tr class="even:bg-grumpy-dark">
            <td><%= u.name %></td>
            <td><%= u.email %></td>
            <td><%= u.preferred_language %></td>
            <td><%= u.created_at.toISOString().slice(0,10) %></td>
            <td><%= u.deactivated ? 'Deactivated' : 'Active' %></td>
            <td>
              <form method="post" action="/admin/users/<%= u.id %>/toggle" style="display:inline">
                <button type="submit" class="text-grumpy-orange underline hover:text-grumpy-offwhite transition"><%= u.deactivated ? 'Reactivate' : 'Deactivate' %></button>
              </form>
              <form method="post" action="/admin/users/<%= u.id %>/delete" style="display:inline" onsubmit="return confirm('Delete user?')">
                <button type="submit" class="text-red-700 underline ml-2 hover:text-grumpy-orange transition">Delete</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <a href="/" class="text-grumpy-orange underline hover:text-grumpy-offwhite transition">Back to Dashboard</a>
  </main>
  <%- include('partials/footer') %>
  <nav class="bg-grumpy-dark2 shadow mt-4 rounded-2xl">
    <ul class="flex space-x-4 p-4">
      <li><a href="/admin/users" class="text-grumpy-orange hover:underline">Users</a></li>
      <li><a href="/admin/translations" class="text-grumpy-orange hover:underline">Translation Management</a></li>
    </ul>
  </nav>
</body>
</html>
